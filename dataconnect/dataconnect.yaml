specVersion: "v1beta"
serviceId: "bus-booking"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "bus-booking-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
